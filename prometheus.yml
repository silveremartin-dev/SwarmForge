global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "swarmforge-server"
    metrics_path: "/actuator/prometheus"
    static_configs:
      - targets: ["server:8081"] # Spring Boot Actuator port
